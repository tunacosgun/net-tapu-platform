export enum ParcelStatus {
  DRAFT = 'draft',
  ACTIVE = 'active',
  DEPOSIT_TAKEN = 'deposit_taken',
  SOLD = 'sold',
  WITHDRAWN = 'withdrawn',
}

export enum ImageStatus {
  UPLOADING = 'uploading',
  PROCESSING = 'processing',
  READY = 'ready',
  FAILED = 'failed',
}
